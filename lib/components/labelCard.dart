import 'package:flutter/material.dart';

class LabelCard extends StatelessWidget {
  final IconData icon;
  final String title, name;

  LabelCard(this.icon, this.title, this.name);

  @override
  Widget build(BuildContext context) {
    return Stack(
        //   alignment: AlignmentGeometry,
        );
  }
}
